model:
  base_params:
    model_args: "pretrained=levinius/MNLP_M3_dpo_model,revision=main"
    dtype: "float16"
    compile: false

    ref_free_norm: "none"

  # Ignore this section, do not modify!
  merged_weights:
    delta_weights: false
    adapter_weights: false
    base_model: null
  generation:
    temperature: 0.0